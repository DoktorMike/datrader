% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portfolio.R
\name{value}
\alias{value}
\alias{value.Portfolio}
\title{Generic to estimate value of an object x}
\usage{
value(x, ...)

\method{value}{Portfolio}(x, currprices, ...)
}
\arguments{
\item{x}{the object to operate on}

\item{...}{additional arguments to pass to specific method}

\item{currprices}{the prices that represents the market}
}
\value{
the estimated value as a scalar
}
\description{
Gives and indication of the value of the object given
a set of arguments.
}
\section{Methods (by class)}{
\itemize{
\item \code{Portfolio}: calculates the value of a Portfolio
}}

\examples{
library(datrader)
mypath <- system.file('extdata', package = 'datrader')
mylist <- loadExistingInstruments(mypath)

p <- Portfolio(
  holdings=structure(rep(1,length(mylist)), .Names=names(mylist)),
  buydates=sapply(mylist, function(x) tail(index(x), 10)[1]),
  buyprices=sapply(mylist, function(x) tail(Cl(x), 10)[1]),
  fees=structure(rep(1,length(mylist)), .Names=names(mylist)) )

value(p, sapply(mylist, function(x) tail(Cl(x), 1)))
}
